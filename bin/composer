#!/usr/bin/env bash
# Run Composer commands in the container
# Usage: bin/composer <command>
# Example: bin/composer install

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

"${SCRIPT_DIR}/cli" composer "$@"
