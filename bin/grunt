#!/usr/bin/env bash
# Run grunt commands in the container
# Usage: bin/grunt <command>
# Example: bin/grunt watch

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

bin/cli grunt "$@"
