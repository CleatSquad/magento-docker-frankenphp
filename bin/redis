#!/usr/bin/env bash
# Access Valkey/Redis CLI
# Usage: bin/redis [command]
# Example: bin/redis KEYS "*"

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

docker compose exec valkey redis-cli "$@"
