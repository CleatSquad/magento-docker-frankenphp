#!/usr/bin/env bash
# Show container status
# Usage: bin/status [profile]
# Default profile: dev

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

PROFILE="${1:-dev}"

docker compose --profile "${PROFILE}" ps
