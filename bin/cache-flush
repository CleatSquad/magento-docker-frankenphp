#!/usr/bin/env bash
# Flush Magento cache
# Usage: bin/cache-flush

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

echo "🧹 Flushing Magento cache..."
bin/magento cache:flush "$@"
echo "✅ Cache flushed successfully!"
