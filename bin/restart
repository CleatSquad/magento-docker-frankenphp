#!/usr/bin/env bash
# Restart Docker containers
# Usage: bin/restart

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "${PROJECT_ROOT}"

bin/stop "$@"
bin/start "$@"
